import Card from "./Card";
import FormField from "./FormField";
import Loader from "./Loader";

//We import the components and we send them like a objects to import in the main component.

export {
    Card,
    FormField,
    Loader
}